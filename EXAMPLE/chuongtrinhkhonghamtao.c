//#include <stdio.h>
//#include <math.h>
#include <HamTao.h>

int main(){
    int a,b,c,d;
    nhap(&a);
    nhap(&b);
    nhap(&c);
    nhap(&d);
    tinhTong(a,b);
    tinhTong(c,d);
    tinhHieu(a,b);
    tinhHieu(c,d);
    
    return 0;
}